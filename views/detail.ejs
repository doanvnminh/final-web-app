<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi Tiết Nhà - Thuê Nhà 24h</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body>

    <header style="background: #333; color: #fff; padding: 15px 0;">
        <div class="nav-container" style="max-width: 1200px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center; padding: 0 15px;">
            <h2 style="margin: 0;">
                <a href="/" style="color: #fff; text-decoration: none;">🏠 Thuê Nhà 24h</a>
            </h2>
            
            <nav style="display: flex; align-items: center;">
                <a href="/" style="color: #fff; text-decoration: none; margin-left: 20px;">Trang chủ</a>
                <a href="/contact" style="color: #fff; text-decoration: none; margin-left: 20px;">Liên hệ</a>

                <% if(user) { %>
                    <div style="display: flex; align-items: center; margin-left: 20px; background: rgba(255,255,255,0.1); padding: 5px 15px; border-radius: 20px;">
                        <img src="https://ui-avatars.com/api/?name=<%= user.username %>&background=random&color=fff" 
                             alt="Avatar" style="width: 30px; height: 30px; border-radius: 50%; margin-right: 8px;">
                        <span style="color: white; font-weight: bold; margin-right: 10px;"><%= user.username %></span>
                        <a href="/logout" style="color: #ff6b6b; font-size: 13px; text-decoration: none; font-weight: bold;">
                            <i class="fa fa-sign-out-alt"></i> Thoát
                        </a>
                    </div>
                <% } else { %>
                    <a href="/login" style="color: #fff; text-decoration: none; margin-left: 20px; border: 1px solid #fff; padding: 5px 15px; border-radius: 4px;">Đăng nhập</a>
                <% } %>
            </nav>
        </div>
    </header>

    <main style="padding: 20px 0; max-width: 800px; margin: 0 auto;">
        
        <div class="product-detail">
            <% if(house.image) { %>
                <img src="/images/<%= house.image %>" alt="<%= house.title %>" style="width: 100%; max-height: 400px; object-fit: cover; border-radius: 8px; margin-bottom: 20px; box-shadow: 0 4px 8px rgba(0,0,0,0.1);">
            <% } %>

            <h1 style="margin-bottom: 10px;"><%= house.title %></h1>
            <p style="color: #e74c3c; font-weight: bold; font-size: 1.5em; margin-top: 0;">
                Giá: <%= house.price %>
            </p>

            <div style="background: #f9f9f9; padding: 20px; border-radius: 8px; margin: 20px 0; border-left: 5px solid #3498db;">
                <p style="margin: 5px 0;"><strong>📍 Địa chỉ:</strong> <%= house.address %></p>
                <p style="margin: 5px 0;"><strong>📞 Liên hệ chủ nhà:</strong> <%= house.contact_phone %></p>
            </div>

            <h3>Mô tả chi tiết</h3>
            <p style="line-height: 1.6; color: #333;">
                <%= house.desc %>
            </p>

            <hr style="margin: 40px 0; border: 0; border-top: 1px solid #ddd;">

            <div class="comment-section">
                <h3>💬 Bình luận & Đánh giá</h3>
                
                <% if(user) { %>
                    <form action="/house/<%= house.id %>/comment" method="POST" style="background: #f4f4f4; padding: 20px; border-radius: 8px; margin-bottom: 30px;">
                        <div style="margin-bottom: 10px;">
                            <label style="font-weight: bold;">Người đăng:</label>
                            <input type="text" value="<%= user.username %>" disabled style="background: #ddd; border: none; padding: 5px; border-radius: 4px;">
                        </div>

                        <textarea name="content" placeholder="Bạn thấy nhà này thế nào?" required style="width: 100%; padding: 10px; height: 80px; margin-bottom: 10px; border: 1px solid #ccc; border-radius: 4px;"></textarea>
                        
                        <div style="display: flex; justify-content: space-between; align-items: center;">
                            <div>
                                <label>Đánh giá:</label>
                                <select name="rating" style="padding: 5px;">
                                    <option value="5">⭐⭐⭐⭐⭐ (Tuyệt vời)</option>
                                    <option value="4">⭐⭐⭐⭐ (Tốt)</option>
                                    <option value="3">⭐⭐⭐ (Bình thường)</option>
                                    <option value="2">⭐⭐ (Tệ)</option>
                                    <option value="1">⭐ (Rất tệ)</option>
                                </select>
                            </div>
                            <button type="submit" style="background: #2ecc71; color: white; border: none; padding: 8px 15px; cursor: pointer; border-radius: 4px;">Gửi đánh giá</button>
                        </div>
                    </form>
                <% } else { %>
                    <div style="background: #fff3cd; color: #856404; padding: 15px; border-radius: 8px; margin-bottom: 30px; text-align: center; border: 1px solid #ffeeba;">
                        🔒 <a href="/login" style="color: #856404; font-weight: bold;">Đăng nhập</a> để gửi bình luận.
                    </div>
                <% } %>

                <div class="comments-list">
                    <% if(comments.length === 0) { %>
                        <p style="color: #999; font-style: italic; text-align: center;">Chưa có đánh giá nào. Hãy là người đầu tiên!</p>
                    <% } else { %>
                        <% comments.forEach(comment => { %>
                            <div style="border-bottom: 1px solid #eee; padding: 15px 0; display: flex; gap: 15px;">
                                <img src="https://ui-avatars.com/api/?name=<%= comment.name %>&background=random&color=fff&size=40" 
                                     alt="User" style="border-radius: 50%; width: 40px; height: 40px;">
                                
                                <div style="flex: 1;">
                                    <div style="display: flex; justify-content: space-between; margin-bottom: 5px;">
                                        <strong style="font-size: 1.1em;"><%= comment.name %></strong>
                                        <span style="color: #f1c40f;">
                                            <% for(let i=0; i<comment.rating; i++) { %>★<% } %>
                                            <% for(let i=comment.rating; i<5; i++) { %>☆<% } %>
                                        </span>
                                    </div>
                                    <p style="margin: 0; color: #555; line-height: 1.5;"><%= comment.content %></p>
                                </div>
                            </div>
                        <% }) %>
                    <% } %>
                </div>
                </div>
        </div>
    </main>

    <footer style="background: #f4f4f4; text-align: center; padding: 20px; margin-top: 50px;">
        <p>&copy; 2025 Website Thuê Nhà. Bài tập lớn Lập trình Web.</p>
    </footer>

    <script src="/js/script.js"></script>
</body>
</html>